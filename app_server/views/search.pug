extends layout

block content
    link(rel="stylesheet", href="stylesheets/search_bar.css")
    a h1 避雷针
    
    .row
        .col-lg-2
        .col-lg-8
                #container
                    .search.barx
                        form
                            input.am-input(type="text" placeholder="在此键入查询QQ...")
                            button.am-btn(type="submit")
        .col-lg-2
