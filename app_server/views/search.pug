extends layout

block content
    link(rel="stylesheet", href="/stylesheets/search_bar.css")
    script(src="/javascripts/flr.js")
    block searchMain


    .container#auto-m
        h1.display-3.text-center(onclick="j2where('/')") 避雷针
        h3.lead.text-center 约稿避雷辅助工具
        .row
            .col-lg-2
            .col-lg-8
                +outputSearchNav(enter)
                #container
                    .search.barx
                        form(action="/search" method="" id="searchForm")
                            +outputSearchBarInput(enter, resBody)
                            button(onclick="search()")
                    +outputSearchResult(resBody)
            .col-lg-2